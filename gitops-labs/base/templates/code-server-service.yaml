{{- if has "code-server" .Values.apps }}
apiVersion: v1
kind: Service
metadata:
  name: code-server-service
  namespace: {{ .Values.nombre }}
  labels:
    app: code-server
spec:
  selector:
    app: code-server
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8443
{{- end }}
